{
  "name": "gemini-chatbot",
  "version": "1.0.0",
  "description": "Static Gemini chat with a server-side proxy for hiding the API key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}