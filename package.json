{
  "name": "gemini-chatbot",
  "version": "1.0.0",
  "description": "Static Gemini chat with a server-side proxy for hiding the API key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build-netlify.js",
    "check": "node --check server.js && node --check script.js && node --check netlify/functions/chat-stream.js"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.2",
    "cors": "^2.8.6",
    "dotenv": "^16.4.7",
    "express": "^4.18.2",
    "firebase": "^12.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
